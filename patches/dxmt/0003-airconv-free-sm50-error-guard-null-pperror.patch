diff --git a/src/airconv/dxbc_converter.cpp b/src/airconv/dxbc_converter.cpp
index c1a0f79..6a58193 100644
--- a/src/airconv/dxbc_converter.cpp
+++ b/src/airconv/dxbc_converter.cpp
@@ -907,19 +907,25 @@ AIRCONV_API int SM50Initialize(
   if (ppError) {
     *ppError = nullptr;
   }
-  auto errorObj = new SM50ErrorInternal();
+  auto errorObj = std::make_unique<SM50ErrorInternal>();
   llvm::raw_svector_ostream errorOut(errorObj->buf);
+  // Hand the error object to the caller only when one is provided; otherwise it
+  // is released with the unique_ptr on scope exit (also covers success paths).
+  auto set_error = [&]() {
+    if (ppError)
+      *ppError = (sm50_error_t)errorObj.release();
+  };
 
   if (ppShader == nullptr) {
     errorOut << "ppShader can not be null\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
   CDXBCParser DXBCParser;
   if (DXBCParser.ReadDXBC(pBytecode, BytecodeSize) != S_OK) {
     errorOut << "Invalid DXBC bytecode\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -929,7 +935,7 @@ AIRCONV_API int SM50Initialize(
   }
   if (codeBlobIdx == DXBC_BLOB_NOT_FOUND) {
     errorOut << "Invalid DXBC bytecode: shader blob not found\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
   const void *codeBlob = DXBCParser.GetBlob(codeBlobIdx);
@@ -940,13 +946,13 @@ AIRCONV_API int SM50Initialize(
   CSignatureParser inputParser;
   if (DXBCGetInputSignature(pBytecode, &inputParser) != S_OK) {
     errorOut << "Invalid DXBC bytecode: input signature not found\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
   CSignatureParser5 outputParser;
   if (DXBCGetOutputSignature(pBytecode, &outputParser) != S_OK) {
     errorOut << "Invalid DXBC bytecode: output signature not found\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1167,7 +1173,7 @@ AIRCONV_API int SM50Initialize(
       auto threads_per_patch = next_pow2(sm50_shader->hull_maximum_threads_per_patch);
       if (threads_per_patch > 32) {
         errorOut << "Threadgroup size of tessellation pipeline is too large.";
-        *ppError = (sm50_error_t)errorObj;
+        set_error();
         return 1;
       }
       auto patch_per_group = 32 / threads_per_patch;
@@ -1179,7 +1185,7 @@ AIRCONV_API int SM50Initialize(
             max_tesselation_factor = std::max(max_tesselation_factor - 2.0f, 1.0f);
           } else {
             errorOut << "Payload size of tessellation pipeline is too large.";
-            *ppError = (sm50_error_t)errorObj;
+            set_error();
             return 1;
           }
         } else {
@@ -1265,11 +1271,17 @@ AIRCONV_API int SM50Compile(
   if (ppError) {
     *ppError = nullptr;
   }
-  auto errorObj = new SM50ErrorInternal();
+  auto errorObj = std::make_unique<SM50ErrorInternal>();
   llvm::raw_svector_ostream errorOut(errorObj->buf);
+  // Hand the error object to the caller only when one is provided; otherwise it
+  // is released with the unique_ptr on scope exit (also covers success paths).
+  auto set_error = [&]() {
+    if (ppError)
+      *ppError = (sm50_error_t)errorObj.release();
+  };
   if (ppBitcode == nullptr) {
     errorOut << "ppBitcode can not be null\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1289,7 +1301,7 @@ AIRCONV_API int SM50Compile(
     llvm::handleAllErrors(std::move(err), [&](const UnsupportedFeature &u) {
       errorOut << u.msg;
     });
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1326,11 +1338,17 @@ AIRCONV_API int SM50CompileTessellationPipelineHull(
   if (ppError) {
     *ppError = nullptr;
   }
-  auto errorObj = new SM50ErrorInternal();
+  auto errorObj = std::make_unique<SM50ErrorInternal>();
   llvm::raw_svector_ostream errorOut(errorObj->buf);
+  // Hand the error object to the caller only when one is provided; otherwise it
+  // is released with the unique_ptr on scope exit (also covers success paths).
+  auto set_error = [&]() {
+    if (ppError)
+      *ppError = (sm50_error_t)errorObj.release();
+  };
   if (ppBitcode == nullptr) {
     errorOut << "ppBitcode can not be null\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1352,7 +1370,7 @@ AIRCONV_API int SM50CompileTessellationPipelineHull(
     llvm::handleAllErrors(std::move(err), [&](const UnsupportedFeature &u) {
       errorOut << u.msg;
     });
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1390,11 +1408,17 @@ AIRCONV_API int SM50CompileTessellationPipelineDomain(
   if (ppError) {
     *ppError = nullptr;
   }
-  auto errorObj = new SM50ErrorInternal();
+  auto errorObj = std::make_unique<SM50ErrorInternal>();
   llvm::raw_svector_ostream errorOut(errorObj->buf);
+  // Hand the error object to the caller only when one is provided; otherwise it
+  // is released with the unique_ptr on scope exit (also covers success paths).
+  auto set_error = [&]() {
+    if (ppError)
+      *ppError = (sm50_error_t)errorObj.release();
+  };
   if (ppBitcode == nullptr) {
     errorOut << "ppBitcode can not be null\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1417,7 +1441,7 @@ AIRCONV_API int SM50CompileTessellationPipelineDomain(
     llvm::handleAllErrors(std::move(err), [&](const UnsupportedFeature &u) {
       errorOut << u.msg;
     });
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1455,11 +1479,17 @@ AIRCONV_API int SM50CompileGeometryPipelineVertex(
   if (ppError) {
     *ppError = nullptr;
   }
-  auto errorObj = new SM50ErrorInternal();
+  auto errorObj = std::make_unique<SM50ErrorInternal>();
   llvm::raw_svector_ostream errorOut(errorObj->buf);
+  // Hand the error object to the caller only when one is provided; otherwise it
+  // is released with the unique_ptr on scope exit (also covers success paths).
+  auto set_error = [&]() {
+    if (ppError)
+      *ppError = (sm50_error_t)errorObj.release();
+  };
   if (ppBitcode == nullptr) {
     errorOut << "ppBitcode can not be null\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1481,7 +1511,7 @@ AIRCONV_API int SM50CompileGeometryPipelineVertex(
     llvm::handleAllErrors(std::move(err), [&](const UnsupportedFeature &u) {
       errorOut << u.msg;
     });
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1518,11 +1548,17 @@ AIRCONV_API int SM50CompileGeometryPipelineGeometry(
   if (ppError) {
     *ppError = nullptr;
   }
-  auto errorObj = new SM50ErrorInternal();
+  auto errorObj = std::make_unique<SM50ErrorInternal>();
   llvm::raw_svector_ostream errorOut(errorObj->buf);
+  // Hand the error object to the caller only when one is provided; otherwise it
+  // is released with the unique_ptr on scope exit (also covers success paths).
+  auto set_error = [&]() {
+    if (ppError)
+      *ppError = (sm50_error_t)errorObj.release();
+  };
   if (ppBitcode == nullptr) {
     errorOut << "ppBitcode can not be null\0";
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
@@ -1545,7 +1581,7 @@ AIRCONV_API int SM50CompileGeometryPipelineGeometry(
     llvm::handleAllErrors(std::move(err), [&](const UnsupportedFeature &u) {
       errorOut << u.msg;
     });
-    *ppError = (sm50_error_t)errorObj;
+    set_error();
     return 1;
   }
 
